
const url = "https://esn360.azurewebsites.net"
const app = '/esn360/'

export const environment = {
  production: true,
  apiUrl: url + app + 'api',
  divUrl: url + app + 'div',
  extractUrl: url + ':8300',
};
